{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "deploy": {
    "startCommand": "cd apps/api && mkdir -p /root/.claude && echo true > /root/.claude/.tosAccepted && echo '{}' > /root/.claude/settings.json && npx tsx src/index.ts"
  }
}
